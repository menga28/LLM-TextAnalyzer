<source>
  @type tail
  path /fluentd/dataset/*.log
  pos_file /fluentd/log/dataset.pos
  tag dataset.*
  format json
</source>

<match dataset.*>
  @type http
  endpoint http://arangodb:8529/_api/import?collection=my_collection
  http_method post
  format json
</match>
